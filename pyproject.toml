[project]
name = "openinteriorcad"
version = "0.1.0"
description = "Open-source semantic CAD platform for interior and furniture design"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 88
target-version = "py311"